<form method="post" action="{{ path('app_docteur_delete', {'id': docteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ docteur.id) }}">
    <button class="btn">Delete</button>
</form>
