{% extends 'base.html.twig' %}

{% block title %}Admin index{% endblock %}

{% block body %}
<br><br><br><br><br><br><br>
<style>
table,td,tr,th {
border: 1px Solid black;
padding:10px;
}
a{
font-weight: bold;
color:green;
}
h1{
text-align:center;
font-weight: bold;
}
section
{
margin-left:20px;
}
</style>
    <h1>Admin index</h1>
<section id="contacter_nous" class="contact-us-single">
    <div class="row no-margin">
        <div  class="col-sm-12 cop-ck">
    <table class="table">
        <thead><div class="w-4/5 m-auto text-center">
        
        <div class="py-12 border-b border-gray-200">
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Email</th>
                <th>Traitement</th>
                
            </tr>
        </thead>
        <tbody>
        {% for admin in user %}
         {% if admin.roles[0]=='ROLE_ADMIN' %}  
            <tr>
              <td>{{ admin.id }}</td>
                <td>{{ admin.nom }}</td>
                <td>{{ admin.prenom }}</td>
                <td>{{ admin.email }}</td>
              
               
                <td>
                    <a href="{{ path('app_user_show', {'id': admin.id}) }}">show</a> |
                    <a href="{{ path('app_user_edit', {'id': admin.id}) }}">edit</a>
                </td>
                
            </tr>
            
        {% else %}
           
            {% endif %}
        {% endfor %}
        </tbody>
    </table>
    </div>
    </div>
    </section>
</div>
</div>
<br>
    <a href="{{ path('app_register_admin') }}" style="margin-left:20px;">Create new Admin</a>
    <br><br><br><br><br><br><br><br><br><br>
{% endblock %}
